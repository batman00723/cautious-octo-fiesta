-- WARNING: This schema is for context only and is not meant to be run.
-- Table order and constraints may not be valid for execution.

CREATE TABLE public.django_migrations (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  app character varying NOT NULL,
  name character varying NOT NULL,
  applied timestamp with time zone NOT NULL,
  CONSTRAINT django_migrations_pkey PRIMARY KEY (id)
);
CREATE TABLE public.django_content_type (
  id integer GENERATED ALWAYS AS IDENTITY NOT NULL,
  app_label character varying NOT NULL,
  model character varying NOT NULL,
  CONSTRAINT django_content_type_pkey PRIMARY KEY (id)
);
CREATE TABLE public.auth_permission (
  id integer GENERATED ALWAYS AS IDENTITY NOT NULL,
  name character varying NOT NULL,
  content_type_id integer NOT NULL,
  codename character varying NOT NULL,
  CONSTRAINT auth_permission_pkey PRIMARY KEY (id),
  CONSTRAINT auth_permission_content_type_id_2f476e4b_fk_django_co FOREIGN KEY (content_type_id) REFERENCES public.django_content_type(id)
);
CREATE TABLE public.auth_group (
  id integer GENERATED ALWAYS AS IDENTITY NOT NULL,
  name character varying NOT NULL UNIQUE,
  CONSTRAINT auth_group_pkey PRIMARY KEY (id)
);
CREATE TABLE public.auth_group_permissions (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  group_id integer NOT NULL,
  permission_id integer NOT NULL,
  CONSTRAINT auth_group_permissions_pkey PRIMARY KEY (id),
  CONSTRAINT auth_group_permissions_group_id_b120cbf9_fk_auth_group_id FOREIGN KEY (group_id) REFERENCES public.auth_group(id),
  CONSTRAINT auth_group_permissio_permission_id_84c5c92e_fk_auth_perm FOREIGN KEY (permission_id) REFERENCES public.auth_permission(id)
);
CREATE TABLE public.auth_user (
  id integer GENERATED ALWAYS AS IDENTITY NOT NULL,
  password character varying NOT NULL,
  last_login timestamp with time zone,
  is_superuser boolean NOT NULL,
  username character varying NOT NULL UNIQUE,
  first_name character varying NOT NULL,
  last_name character varying NOT NULL,
  email character varying NOT NULL,
  is_staff boolean NOT NULL,
  is_active boolean NOT NULL,
  date_joined timestamp with time zone NOT NULL,
  CONSTRAINT auth_user_pkey PRIMARY KEY (id)
);
CREATE TABLE public.auth_user_groups (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  user_id integer NOT NULL,
  group_id integer NOT NULL,
  CONSTRAINT auth_user_groups_pkey PRIMARY KEY (id),
  CONSTRAINT auth_user_groups_user_id_6a12ed8b_fk_auth_user_id FOREIGN KEY (user_id) REFERENCES public.auth_user(id),
  CONSTRAINT auth_user_groups_group_id_97559544_fk_auth_group_id FOREIGN KEY (group_id) REFERENCES public.auth_group(id)
);
CREATE TABLE public.auth_user_user_permissions (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  user_id integer NOT NULL,
  permission_id integer NOT NULL,
  CONSTRAINT auth_user_user_permissions_pkey PRIMARY KEY (id),
  CONSTRAINT auth_user_user_permissions_user_id_a95ead1b_fk_auth_user_id FOREIGN KEY (user_id) REFERENCES public.auth_user(id),
  CONSTRAINT auth_user_user_permi_permission_id_1fbb5f2c_fk_auth_perm FOREIGN KEY (permission_id) REFERENCES public.auth_permission(id)
);
CREATE TABLE public.django_admin_log (
  id integer GENERATED ALWAYS AS IDENTITY NOT NULL,
  action_time timestamp with time zone NOT NULL,
  object_id text,
  object_repr character varying NOT NULL,
  action_flag smallint NOT NULL CHECK (action_flag >= 0),
  change_message text NOT NULL,
  content_type_id integer,
  user_id integer NOT NULL,
  CONSTRAINT django_admin_log_pkey PRIMARY KEY (id),
  CONSTRAINT django_admin_log_content_type_id_c4bce8eb_fk_django_co FOREIGN KEY (content_type_id) REFERENCES public.django_content_type(id),
  CONSTRAINT django_admin_log_user_id_c564eba6_fk_auth_user_id FOREIGN KEY (user_id) REFERENCES public.auth_user(id)
);
CREATE TABLE public.companies (
  organization_number character varying NOT NULL,
  name text NOT NULL,
  established_date date,
  registered_date date,
  vat_registered_date date,
  employee_count integer NOT NULL,
  website text,
  business_address jsonb,
  business_postal_code character varying,
  business_city text,
  business_country_code character varying,
  postal_address jsonb,
  postal_code character varying,
  city text,
  country_code character varying,
  institutional_sector_code character varying,
  institutional_sector_name text,
  is_vat_registered boolean NOT NULL,
  is_registered_business_register boolean NOT NULL,
  is_registered_foundation boolean NOT NULL,
  is_registered_voluntary boolean NOT NULL,
  is_bankrupt boolean NOT NULL,
  is_under_liquidation boolean NOT NULL,
  is_under_forced_liquidation boolean NOT NULL,
  purpose text,
  search_vector tsvector,
  organization_type_code character varying,
  CONSTRAINT companies_pkey PRIMARY KEY (organization_number),
  CONSTRAINT companies_organization_type_co_a67c4050_fk_organizat FOREIGN KEY (organization_type_code) REFERENCES public.organization_types(code)
);
CREATE TABLE public.contact_messages (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  name character varying NOT NULL,
  email character varying NOT NULL,
  phone_number character varying,
  message text NOT NULL,
  created_at timestamp with time zone NOT NULL,
  CONSTRAINT contact_messages_pkey PRIMARY KEY (id)
);
CREATE TABLE public.deletion_requests (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  organization_number character varying NOT NULL,
  name character varying NOT NULL,
  email character varying NOT NULL,
  reason text,
  created_at timestamp with time zone NOT NULL,
  CONSTRAINT deletion_requests_pkey PRIMARY KEY (id)
);
CREATE TABLE public.industries (
  code character varying NOT NULL,
  description text NOT NULL,
  parent_code character varying,
  CONSTRAINT industries_pkey PRIMARY KEY (code)
);
CREATE TABLE public.municipalities (
  municipality_code character varying NOT NULL,
  name text NOT NULL,
  CONSTRAINT municipalities_pkey PRIMARY KEY (municipality_code)
);
CREATE TABLE public.organization_types (
  code character varying NOT NULL,
  description text NOT NULL,
  CONSTRAINT organization_types_pkey PRIMARY KEY (code)
);
CREATE TABLE public.people (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  first_name text,
  middle_name text,
  last_name text,
  birth_date date,
  is_deceased boolean NOT NULL,
  CONSTRAINT people_pkey PRIMARY KEY (id)
);
CREATE TABLE public.role_types (
  code character varying NOT NULL,
  description text NOT NULL,
  group_code character varying,
  group_description text,
  CONSTRAINT role_types_pkey PRIMARY KEY (code)
);
CREATE TABLE public.company_locations (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  organization_number character varying NOT NULL UNIQUE,
  name text NOT NULL,
  website text,
  employee_count integer NOT NULL,
  business_address jsonb,
  postal_code character varying,
  city text,
  country_code character varying,
  postal_address jsonb,
  registered_date date,
  established_date date,
  company_id character varying NOT NULL,
  organization_type_code character varying,
  CONSTRAINT company_locations_pkey PRIMARY KEY (id),
  CONSTRAINT company_locations_company_id_7f226ab1_fk_companies FOREIGN KEY (company_id) REFERENCES public.companies(organization_number),
  CONSTRAINT company_locations_organization_type_co_17f31cdb_fk_organizat FOREIGN KEY (organization_type_code) REFERENCES public.organization_types(code)
);
CREATE TABLE public.company_roles (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  is_active boolean NOT NULL,
  company_id character varying NOT NULL,
  holding_company_id character varying,
  person_id bigint,
  role_type_code character varying NOT NULL,
  CONSTRAINT company_roles_pkey PRIMARY KEY (id),
  CONSTRAINT company_roles_company_id_7f6e605f_fk_companies FOREIGN KEY (company_id) REFERENCES public.companies(organization_number),
  CONSTRAINT company_roles_holding_company_id_1beab4e4_fk_companies FOREIGN KEY (holding_company_id) REFERENCES public.companies(organization_number),
  CONSTRAINT company_roles_person_id_7ec67f39_fk_people_id FOREIGN KEY (person_id) REFERENCES public.people(id),
  CONSTRAINT company_roles_role_type_code_0cc09ea0_fk_role_types_code FOREIGN KEY (role_type_code) REFERENCES public.role_types(code)
);
CREATE TABLE public.financial_statements (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  journal_number character varying,
  financial_year integer NOT NULL,
  period_start date,
  period_end date,
  currency character varying NOT NULL,
  accounting_type character varying,
  statement_plan character varying,
  is_liquidation_statement boolean NOT NULL,
  is_small_business boolean NOT NULL,
  accounting_rules character varying,
  audit_exempt boolean NOT NULL,
  total_assets bigint,
  total_equity bigint,
  operating_revenue bigint,
  operating_profit bigint,
  net_profit bigint,
  raw_json jsonb,
  company_id character varying NOT NULL,
  CONSTRAINT financial_statements_pkey PRIMARY KEY (id),
  CONSTRAINT financial_statements_company_id_cca7bb1c_fk_companies FOREIGN KEY (company_id) REFERENCES public.companies(organization_number)
);
CREATE TABLE public.company_industries (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  is_primary boolean NOT NULL,
  company_id character varying NOT NULL,
  industry_id character varying NOT NULL,
  CONSTRAINT company_industries_pkey PRIMARY KEY (id),
  CONSTRAINT company_industries_company_id_8ba2f0cb_fk_companies FOREIGN KEY (company_id) REFERENCES public.companies(organization_number),
  CONSTRAINT company_industries_industry_id_a3934541_fk_industries_code FOREIGN KEY (industry_id) REFERENCES public.industries(code)
);
CREATE TABLE public.django_session (
  session_key character varying NOT NULL,
  session_data text NOT NULL,
  expire_date timestamp with time zone NOT NULL,
  CONSTRAINT django_session_pkey PRIMARY KEY (session_key)
);